#ifndef GB_MATH_H
#define GB_MATH_H

#include <stdint.h>

uint8_t multiplication(uint8_t a, uint8_t b);

uint8_t division(uint8_t a, uint8_t b);

uint8_t calculateIndex(uint8_t x, uint8_t y, uint8_t size);

uint8_t calculateMatrix(uint8_t rows);

#endif